# מדריך עדכון תוכן — אתר עו"ד אופיר עומר

מדריך זה מיועד לעדכון תוכן האתר **בלי צורך בידע בתכנות**. כל התוכן נמצא בקבצי טקסט בפורמט JSON תחת התיקייה `src/content` ותיקיית `src/_data`. עורכים את הקובץ, שומרים, ומעלים (`git push`) — האתר נבנה ומתעדכן אוטומטית.

חשוב: כל קובץ הוא טקסט רגיל. יש לשמור על מבנה ה-JSON (סוגריים מסולסלים `{ }`, מירכאות `" "`, פסיקים `,`) בדיוק כפי שהוא, ולשנות רק את הטקסט שבתוך המירכאות.

---

## הוספת/עריכת תיק נבחר

תיקייה: `src/content/cases/`

כל תיק הוא קובץ JSON נפרד, ומקבל דף משלו בכתובת `/cases/<slug>/`. כדי להוסיף תיק חדש — יוצרים העתק של קובץ קיים (למשל `src/content/cases/labor-law-tzemach-5080-03-23.json`) עם שם קובץ חדש, וממלאים את השדות:

```json
{
  "slug": "real-estate-example-case",
  "category": "real-estate",
  "field_he": "מקרקעין",
  "field_en": "Real Estate",
  "title_he": "כותרת/ציטוט התיק בעברית",
  "title_en": "Case title/citation in English",
  "summary_he": "משפט או שניים שמסכמים את התוצאה — זה הטקסט שמוצג בכרטיס בעמוד תיקים נבחרים",
  "summary_en": "One or two sentences summarizing the outcome — this is what shows on the card on the Selected Cases page",
  "content_he": "פסקה ראשונה בעברית.\n\nפסקה שנייה בעברית.",
  "content_en": "First paragraph in English.\n\nSecond paragraph in English."
}
```

- `slug` קובע את כתובת ה-URL של התיק (`/cases/<slug>/`) — יש להשתמש רק באותיות אנגליות, מספרים ומקפים, ולוודא שהוא **שונה מכל תיק אחר בתיקייה** (שני תיקים עם אותו `slug` יגרמו לשגיאה בבנייה). מקובל (אך לא חובה) לתת לקובץ עצמו את אותו שם כמו ה-`slug`.
- `category` הוא מזהה טכני (באנגלית, ללא רווחים) המשמש לסינון בעמוד "תיקים נבחרים". תיקים עם אותו `category` מקובצים יחד.
- להפרדה בין פסקאות בתוך `content_he`/`content_en`, יש להשאיר שורה ריקה בין הפסקאות (`\n\n`).

**שימו לב:** כל קובץ `.json` בתיקייה הזו נטען אוטומטית כתיק אמיתי ומוצג באתר — אין ליצור כאן קובץ "תבנית" ריק לצורך עיון בלבד, כי הוא יתפרסם כתיק אמיתי (ריק) באתר. כדי להתחיל תיק חדש, תמיד להעתיק קובץ קיים ולהחליף את התוכן.

---

## עדכון רשימת לקוחות

קובץ: `src/content/clients.json`

האתר מציג את הלקוחות כרשת לוגואים בעמוד "לקוחות". אם הרשימה ריקה (`[]`), מוצג במקום זאת משפט כללי. כל לקוח הוא אובייקט עם השדות הבאים:

```json
[
  { "name": "שם החברה", "logoUrl": "/assets/images/clients/company-name.svg", "website": "https://example.com/" },
  { "name": "לקוח בלי אתר או לוגו" }
]
```

- `name` — שדה חובה, שם הלקוח.
- `logoUrl` — שדה אופציונלי, נתיב לקובץ הלוגו (SVG/PNG/JPG) בתיקייה `src/assets/images/clients/`. אם לא מוזן, מוצג שם הלקוח כטקסט בלבד במקום לוגו.
- `website` — שדה אופציונלי, כתובת האתר של הלקוח. אם מוזן, הלוגו הופך לקישור הנפתח בלשונית חדשה. ללקוח בלי אתר (או שלא רוצים לקשר אליו) — פשוט משמיטים את השדה.

**אין להוסיף שמות לקוחות שלא אושרו לפרסום בפועל.**

---

## עדכון פרטי המשרד

קובץ: `src/_data/site.json`

כאן נמצאים הפרטים הכלליים של המשרד: שם, כתובת, טלפון, דוא"ל, מספר WhatsApp, וכתובת האתר. יש להחליף כל `[להשלמה]` בפרט האמיתי. שדה `whatsapp` — אם הוא ריק (`""`), כפתור הוואטסאפ הצף לא יוצג באתר; ברגע שמוזן מספר (בפורמט בינלאומי, למשל `972501234567`), הכפתור יופיע אוטומטית.

---

## תחומי עיסוק

קובץ: `src/content/practice-areas.json`

שינוי טקסט קיים או עריכת התיאורים אפשריים ישירות בקובץ. **הוספת** תחום עיסוק חדש דורשת גם עדכון קטן בתפריט הניווט (`src/_data/strings.json`) — לכך מומלץ להיעזר במפתח.

---

## תמונות

תיקייה: `src/assets/images/`

תמונת הפורטרט הראשית (`OfirLawMain.jpeg`) כבר מוצבת, ומוצגת אוטומטית בדף הבית ובדף "אודות" — שני הדפים קוראים את הנתיב מהשדה `portraitLarge` בקובץ `src/_data/site.json`, כך שהחלפת התמונה במקום אחד (העלאת קובץ חדש לתיקייה ועדכון הנתיב בקובץ הזה) מעדכנת את שני הדפים בבת אחת.

תמונת השיתוף לרשתות (`og-image.jpg`, בגודל 1200×630 פיקסלים) כבר מוצבת ומופיעה כשמשתפים קישור לאתר בוואטסאפ/פייסבוק/לינקדאין. להחלפתה — מעלים קובץ JPG/PNG חדש באותו גודל לתיקייה ומעדכנים את הנתיב ב-`site.json`, שדה `ogImage`.

---

## הצהרת נגישות

קובץ: `src/_data/site.json`, מפתח `accessibility` — כאן מוגדרים פרטי רכז/ת הנגישות (`coordinatorName_he`/`coordinatorName_en`, `coordinatorPhone`, `coordinatorEmail`), תאריך עדכון ההצהרה (`statementDate_he`/`statementDate_en`), ורמת התאימות בפועל (`conformanceLevel_he`/`conformanceLevel_en`). רשימת "מגבלות ידועות" בעמוד עצמו (`src/accessibility/index.njk` ו-`src/en/accessibility/index.njk`) כתובה ישירות בקוד ה-HTML של העמוד, לא בקובץ הנתונים — יש לעדכן אותה בכל פעם שמגבלה נגישות אמיתית מתוקנת או מתגלה. **מומלץ לבצע בדיקת נגישות מקצועית חד-פעמית** מול בודק חיצוני מוסמך לפני שינוי רמת התאימות המוצהרת מ"חלקית" ל"מלאה".

---

## מדיניות פרטיות

קבצים: `src/privacy-policy/index.njk` (עברית) ו-`src/en/privacy-policy/index.njk` (אנגלית), ותאריך העדכון ב-`src/_data/site.json` (מפתחות `privacy.lastUpdated_he` ו-`privacy.lastUpdated_en` — יש לעדכן את שניהם, כל אחד בפורמט התאריך המקובל בשפתו).

הטקסט הנוכחי מתאר במדויק את המצב הטכני של האתר כפי שנבנה (טופס יצירת קשר הפועל כרגע כ-mailto בלבד, ללא שימוש בכלי ניתוח נתונים או עוגיות, אחסון ב-GitHub Pages, שימוש ב-Google Fonts). **חשוב לעדכן עמוד זה בכל פעם שמתווסף שירות חדש** (למשל שירות שליחת מיילים כמו Resend/Formspree, כלי אנליטיקס, או פיקסל פרסומי), וכן מומלץ שעו"ד אופיר עומר יאשר את הנוסח הסופי לפני פרסום, מאחר שמדובר במסמך בעל משמעות משפטית.
