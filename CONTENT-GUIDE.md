# מדריך עדכון תוכן — אתר עו"ד אופיר עומר

מדריך זה מיועד לעדכון תוכן האתר **בלי צורך בידע בתכנות**. כל התוכן נמצא בקבצי טקסט בפורמט JSON תחת התיקייה `src/content` ותיקיית `src/_data`. עורכים את הקובץ, שומרים, ומעלים (`git push`) — האתר נבנה ומתעדכן אוטומטית.

חשוב: כל קובץ הוא טקסט רגיל. יש לשמור על מבנה ה-JSON (סוגריים מסולסלים `{ }`, מירכאות `" "`, פסיקים `,`) בדיוק כפי שהוא, ולשנות רק את הטקסט שבתוך המירכאות.

---

## הוספת/עריכת תיק נבחר

תיקייה: `src/content/cases/`

כל תיק הוא קובץ JSON נפרד (למשל `template-real-estate.json`). כדי להוסיף תיק חדש — יוצרים העתק של קובץ קיים עם שם חדש, וממלאים את השדות:

```json
{
  "category": "real-estate",
  "field_he": "מקרקעין",
  "field_en": "Real Estate",
  "challenge_he": "תיאור האתגר בעברית",
  "challenge_en": "Challenge description in English",
  "strategy_he": "תיאור האסטרטגיה בעברית",
  "strategy_en": "Strategy description in English",
  "outcome_he": "תיאור התוצאה בעברית",
  "outcome_en": "Outcome description in English"
}
```

`category` הוא מזהה טכני (באנגלית, ללא רווחים) המשמש לסינון בעמוד "תיקים נבחרים". תיקים עם אותו `category` מקובצים יחד.

---

## הוספת/עריכת מאמר

תיקייה: `src/content/articles/`

כל מאמר הוא קובץ JSON נפרד. יוצרים קובץ חדש (למשל `2026-02-tenant-disputes.json`) עם המבנה הבא:

```json
{
  "slug": "tenant-disputes-2026",
  "category": "real-estate",
  "categoryLabel_he": "מקרקעין",
  "categoryLabel_en": "Real Estate",
  "date": "2026-02-15",
  "title_he": "כותרת המאמר בעברית",
  "title_en": "Article title in English",
  "summary_he": "תקציר קצר בעברית",
  "summary_en": "Short summary in English",
  "content_he": "פסקה ראשונה בעברית.\n\nפסקה שנייה בעברית.",
  "content_en": "First paragraph in English.\n\nSecond paragraph in English."
}
```

- `slug` קובע את כתובת ה-URL של המאמר (`/articles/<slug>/`) — יש להשתמש רק באותיות אנגליות, מספרים ומקפים.
- `date` בפורמט `YYYY-MM-DD`.
- להפרדה בין פסקאות בתוך `content_he`/`content_en`, יש להשאיר שורה ריקה בין הפסקאות (`\n\n`).

---

## עדכון רשימת לקוחות

קובץ: `src/content/clients.json`

כברירת מחדל הקובץ ריק (`[]`) והאתר מציג משפט כללי. כדי להציג שמות לקוחות בפועל (רק אם ניתן לפרסם אותם):

```json
[
  { "name": "שם החברה" },
  { "name": "שם לקוח נוסף" }
]
```

**אין להוסיף שמות לקוחות שלא אושרו לפרסום בפועל.**

---

## עדכון פרטי המשרד

קובץ: `src/_data/site.json`

כאן נמצאים הפרטים הכלליים של המשרד: שם, כתובת, טלפון, דוא"ל, מספר WhatsApp, וכתובת האתר. יש להחליף כל `[להשלמה]` בפרט האמיתי. שדה `whatsapp` — אם הוא ריק (`""`), כפתור הוואטסאפ הצף לא יוצג באתר; ברגע שמוזן מספר (בפורמט בינלאומי, למשל `972501234567`), הכפתור יופיע אוטומטית.

---

## תחומי עיסוק

קובץ: `src/content/practice-areas.json`

שינוי טקסט קיים או עריכת התיאורים אפשריים ישירות בקובץ. **הוספת** תחום עיסוק חדש דורשת גם עדכון קטן בתפריט הניווט (`src/_data/strings.json`) — לכך מומלץ להיעזר במפתח.

---

## תמונות

תיקייה: `src/assets/images/`

תמונת הפורטרט הראשית (`OfirLawMain.jpeg`) כבר מוצבת, ומוצגת אוטומטית בדף הבית ובדף "אודות" — שני הדפים קוראים את הנתיב מהשדה `portraitLarge` בקובץ `src/_data/site.json`, כך שהחלפת התמונה במקום אחד (העלאת קובץ חדש לתיקייה ועדכון הנתיב בקובץ הזה) מעדכנת את שני הדפים בבת אחת.

תמונת השיתוף לרשתות (`og-image-placeholder.svg`) עדיין placeholder, ומומלץ להחליפה בתמונת JPG/PNG בגודל 1200×630 פיקסלים לתאימות מיטבית עם פייסבוק/לינקדאין/וואטסאפ (עדכון הנתיב ב-`site.json`, שדה `ogImage`).

---

## הצהרת נגישות

קובץ: `src/_data/site.json`, מפתח `accessibility` — יש להשלים את פרטי רכז/ת הנגישות, תאריך עדכון ההצהרה, ורמת התאימות בפועל (מומלץ לבצע בדיקת נגישות מקצועית לפני קביעת הרמה הסופית).

---

## מדיניות פרטיות

קבצים: `src/privacy-policy/index.njk` (עברית) ו-`src/en/privacy-policy/index.njk` (אנגלית), ותאריך העדכון ב-`src/_data/site.json` (מפתח `privacy.lastUpdated`).

הטקסט הנוכחי מתאר במדויק את המצב הטכני של האתר כפי שנבנה (טופס יצירת קשר הפועל כרגע כ-mailto בלבד, ללא שימוש בכלי ניתוח נתונים או עוגיות, אחסון ב-GitHub Pages, שימוש ב-Google Fonts). **חשוב לעדכן עמוד זה בכל פעם שמתווסף שירות חדש** (למשל שירות שליחת מיילים כמו Resend/Formspree, כלי אנליטיקס, או פיקסל פרסומי), וכן מומלץ שעו"ד אופיר עומר יאשר את הנוסח הסופי לפני פרסום, מאחר שמדובר במסמך בעל משמעות משפטית.
